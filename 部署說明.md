# 夜市撈魚小遊戲 —— 排行榜/紀錄後端部署說明

這份說明是給 Colin（或接手的工程師）用的，目的是把遊戲的「答題紀錄」和「排行榜」接到一個 Google Sheet 上。全程不用寫程式、不用申請額外帳號，用 One-Forty 現有的 Google 帳號就能做完，大約 10 分鐘。

## 這個架構能做到什麼、不能做到什麼

能做到：每一局遊戲結束（玩完 10 題，或生命值歸零）都會記一筆到 Google Sheet，包含時間、匿名裝置代碼、語言、分數、答對題數、花費秒數、結果。結束頁會顯示分數前 10 名的簡易排行榜。Sheet 上可以看到「總遊玩次數」「有多少不同裝置玩過」「每個裝置玩了幾次、最高分」。

做不到：因為沒有登入機制，沒辦法辨識「真正的某一個人」，只能用「這支手機/瀏覽器」當單位——同一支手機重複玩，會被算成同一個裝置；如果多人共用同一支手機/平板掃碼玩，會被算成同一個裝置。排行榜不是即時更新（玩家要重新整理頁面、或下一次結束遊戲時才會抓到最新排名），不適合當成「現場大螢幕即時競賽榜」使用。

## 步驟

**1. 建立一個新的 Google Sheet**

到 Google Sheet（sheets.google.com）新增一個空白試算表，取名例如「夜市撈魚遊戲紀錄」。

**2. 打開 Apps Script 編輯器**

在試算表選單列：擴充功能（Extensions）→ Apps Script。會開一個新分頁，裡面有一個預設的 `Code.gs` 檔案。

**3. 貼上程式碼**

把預設的內容全部刪掉，貼上這個資料夾裡附上的 `Code.gs` 檔案內容（我已經寫好了，不用修改）。貼完按左上角儲存（磁片圖示）。

**4. 部署成 Web App**

右上角「部署」（Deploy）→「新增部署作業」（New deployment）。
- 類型選「網頁應用程式」（Web app）
- 說明可以隨便填，例如「game-api-v1」
- 「執行身分」選「我」（自己的帳號）
- 「誰可以存取」選「所有人」（Anyone）— 這一步很重要，沒選「所有人」的話遊戲會連不上
- 按「部署」，第一次會跳出 Google 要求授權的畫面，照著授權自己的帳號即可（這是正常的，因為腳本要寫入你自己的試算表）

部署完成後會出現一個網址，長得像：
`https://script.google.com/macros/s/AKfycb.......具体一串字/exec`

把這個網址複製起來。

**5. 把網址填回遊戲檔案**

打開 `one-forty-chinese-game-prototype.html`，找到這一行（在檔案偏前面）：

```js
const API_URL = "";
```

改成：

```js
const API_URL = "貼上你剛剛複製的網址";
```

存檔後重新打開遊戲，玩一局試試看，結束頁應該就會出現排行榜。同時回去看你的 Google Sheet，應該會多一個「Log」分頁，裡面多了一筆資料。

**6.（選用）建立統計分頁**

如果想要一眼看到「總共幾人玩過」「每個人玩了幾次」，回到 Apps Script 編輯器，在上方函式選單選擇 `setupStatsSheet`，按執行（▶️）。跑完後回試算表，會多一個「Stats」分頁，裡面有公式自動算好的統計數字，之後 Log 分頁有新資料，Stats 會自動更新，不用重跑。

## 之後如果要改

- 想改排行榜顯示幾名：`Code.gs` 裡的 `getLeaderboard_()` 函式，把 `slice(0, 10)` 的 10 改成想要的數字。
- 想清空重來：直接刪掉 Google Sheet 裡「Log」分頁的資料列（保留標題列），排行榜和統計會自動歸零。
- 如果之後真的需要即時排行榜（現場大螢幕用），Google Sheet 這個方案就不太夠力了，屆時建議換成 Firebase 之類的即時資料庫，那時候可以再回來找我調整。

---
建立日期：2026-07-23
